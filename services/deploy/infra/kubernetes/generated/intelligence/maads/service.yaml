apiVersion: v1
kind: Service
metadata:
  name: maads
  namespace: intelligence
  labels:
    app: maads
    atlasos.io/managed: "true"
spec:
  selector:
    app: maads
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
