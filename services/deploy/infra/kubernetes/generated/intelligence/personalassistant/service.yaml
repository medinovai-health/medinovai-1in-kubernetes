apiVersion: v1
kind: Service
metadata:
  name: personalassistant
  namespace: intelligence
  labels:
    app: personalassistant
    atlasos.io/managed: "true"
spec:
  selector:
    app: personalassistant
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
