apiVersion: v1
kind: Service
metadata:
  name: medinovai-voice-command-layer
  namespace: intelligence
  labels:
    app: medinovai-voice-command-layer
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-voice-command-layer
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
