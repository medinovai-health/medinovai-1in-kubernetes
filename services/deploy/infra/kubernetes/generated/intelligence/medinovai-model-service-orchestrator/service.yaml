apiVersion: v1
kind: Service
metadata:
  name: medinovai-model-service-orchestrator
  namespace: intelligence
  labels:
    app: medinovai-model-service-orchestrator
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-model-service-orchestrator
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
