apiVersion: v1
kind: Service
metadata:
  name: medinovai-accessibility-checker
  namespace: intelligence
  labels:
    app: medinovai-accessibility-checker
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-accessibility-checker
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
