apiVersion: v1
kind: Service
metadata:
  name: medinovai-ai-scribe
  namespace: intelligence
  labels:
    app: medinovai-ai-scribe
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-ai-scribe
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
