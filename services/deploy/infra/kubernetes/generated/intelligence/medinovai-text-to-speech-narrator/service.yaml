apiVersion: v1
kind: Service
metadata:
  name: medinovai-text-to-speech-narrator
  namespace: intelligence
  labels:
    app: medinovai-text-to-speech-narrator
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-text-to-speech-narrator
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
