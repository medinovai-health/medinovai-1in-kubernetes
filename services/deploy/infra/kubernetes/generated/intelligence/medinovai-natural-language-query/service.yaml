apiVersion: v1
kind: Service
metadata:
  name: medinovai-natural-language-query
  namespace: intelligence
  labels:
    app: medinovai-natural-language-query
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-natural-language-query
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
