apiVersion: v1
kind: Service
metadata:
  name: photoai
  namespace: intelligence
  labels:
    app: photoai
    atlasos.io/managed: "true"
spec:
  selector:
    app: photoai
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
