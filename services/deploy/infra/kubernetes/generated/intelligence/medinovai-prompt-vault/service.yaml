apiVersion: v1
kind: Service
metadata:
  name: medinovai-prompt-vault
  namespace: intelligence
  labels:
    app: medinovai-prompt-vault
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-prompt-vault
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
