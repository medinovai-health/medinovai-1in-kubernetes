apiVersion: v1
kind: Service
metadata:
  name: medinovai-knowledge-graph
  namespace: data
  labels:
    app: medinovai-knowledge-graph
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-knowledge-graph
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
