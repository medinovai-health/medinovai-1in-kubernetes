apiVersion: v1
kind: Service
metadata:
  name: medinovai-data-lake-loader
  namespace: data
  labels:
    app: medinovai-data-lake-loader
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-data-lake-loader
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
