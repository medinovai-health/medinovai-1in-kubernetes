apiVersion: v1
kind: Service
metadata:
  name: medinovai-reseach-fabric
  namespace: data
  labels:
    app: medinovai-reseach-fabric
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-reseach-fabric
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
