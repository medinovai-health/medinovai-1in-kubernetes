apiVersion: v1
kind: Service
metadata:
  name: medinovai-etl-designer
  namespace: data
  labels:
    app: medinovai-etl-designer
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-etl-designer
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
