apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: data
  labels:
    app: database
    atlasos.io/managed: "true"
spec:
  selector:
    app: database
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
