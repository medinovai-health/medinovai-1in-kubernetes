apiVersion: v1
kind: Service
metadata:
  name: ai-coach
  namespace: platform
  labels:
    app: ai-coach
    atlasos.io/managed: "true"
spec:
  selector:
    app: ai-coach
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
