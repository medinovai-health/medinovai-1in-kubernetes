apiVersion: v1
kind: Service
metadata:
  name: vercel-clone
  namespace: platform
  labels:
    app: vercel-clone
    atlasos.io/managed: "true"
spec:
  selector:
    app: vercel-clone
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
