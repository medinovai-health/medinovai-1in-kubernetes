apiVersion: v1
kind: Service
metadata:
  name: procurementpro
  namespace: platform
  labels:
    app: procurementpro
    atlasos.io/managed: "true"
spec:
  selector:
    app: procurementpro
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
