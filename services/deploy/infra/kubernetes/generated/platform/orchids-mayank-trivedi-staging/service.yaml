apiVersion: v1
kind: Service
metadata:
  name: orchids-mayank-trivedi-staging
  namespace: platform
  labels:
    app: orchids-mayank-trivedi-staging
    atlasos.io/managed: "true"
spec:
  selector:
    app: orchids-mayank-trivedi-staging
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
