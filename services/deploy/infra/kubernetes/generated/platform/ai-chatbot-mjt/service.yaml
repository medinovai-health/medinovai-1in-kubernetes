apiVersion: v1
kind: Service
metadata:
  name: ai-chatbot-mjt
  namespace: platform
  labels:
    app: ai-chatbot-mjt
    atlasos.io/managed: "true"
spec:
  selector:
    app: ai-chatbot-mjt
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
