apiVersion: v1
kind: Service
metadata:
  name: medinovai-media
  namespace: platform
  labels:
    app: medinovai-media
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-media
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
