apiVersion: v1
kind: Service
metadata:
  name: hello-world-codex
  namespace: platform
  labels:
    app: hello-world-codex
    atlasos.io/managed: "true"
spec:
  selector:
    app: hello-world-codex
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
