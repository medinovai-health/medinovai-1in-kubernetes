apiVersion: v1
kind: Service
metadata:
  name: qualitymanagementsystem
  namespace: platform
  labels:
    app: qualitymanagementsystem
    atlasos.io/managed: "true"
spec:
  selector:
    app: qualitymanagementsystem
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
