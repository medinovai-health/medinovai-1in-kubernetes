apiVersion: v1
kind: Service
metadata:
  name: autograntpro-cursor
  namespace: platform
  labels:
    app: autograntpro-cursor
    atlasos.io/managed: "true"
spec:
  selector:
    app: autograntpro-cursor
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
