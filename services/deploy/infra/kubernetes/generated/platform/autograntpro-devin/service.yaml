apiVersion: v1
kind: Service
metadata:
  name: autograntpro-devin
  namespace: platform
  labels:
    app: autograntpro-devin
    atlasos.io/managed: "true"
spec:
  selector:
    app: autograntpro-devin
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
