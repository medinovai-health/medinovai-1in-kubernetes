apiVersion: v1
kind: Service
metadata:
  name: myhealthconcierge-android
  namespace: platform
  labels:
    app: myhealthconcierge-android
    atlasos.io/managed: "true"
spec:
  selector:
    app: myhealthconcierge-android
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
