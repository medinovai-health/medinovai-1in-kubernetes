apiVersion: v1
kind: Service
metadata:
  name: protocoldesign
  namespace: platform
  labels:
    app: protocoldesign
    atlasos.io/managed: "true"
spec:
  selector:
    app: protocoldesign
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
