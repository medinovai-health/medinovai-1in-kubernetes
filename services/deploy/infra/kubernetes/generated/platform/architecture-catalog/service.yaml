apiVersion: v1
kind: Service
metadata:
  name: architecture-catalog
  namespace: platform
  labels:
    app: architecture-catalog
    atlasos.io/managed: "true"
spec:
  selector:
    app: architecture-catalog
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
