apiVersion: v1
kind: Service
metadata:
  name: mos-chatbot
  namespace: platform
  labels:
    app: mos-chatbot
    atlasos.io/managed: "true"
spec:
  selector:
    app: mos-chatbot
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
