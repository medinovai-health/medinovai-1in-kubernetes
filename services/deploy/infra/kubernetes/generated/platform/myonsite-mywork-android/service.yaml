apiVersion: v1
kind: Service
metadata:
  name: myonsite-mywork-android
  namespace: platform
  labels:
    app: myonsite-mywork-android
    atlasos.io/managed: "true"
spec:
  selector:
    app: myonsite-mywork-android
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
