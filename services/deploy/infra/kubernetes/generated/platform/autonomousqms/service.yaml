apiVersion: v1
kind: Service
metadata:
  name: autonomousqms
  namespace: platform
  labels:
    app: autonomousqms
    atlasos.io/managed: "true"
spec:
  selector:
    app: autonomousqms
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
