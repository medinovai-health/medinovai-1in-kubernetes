apiVersion: v1
kind: Service
metadata:
  name: autograntpro-manus
  namespace: platform
  labels:
    app: autograntpro-manus
    atlasos.io/managed: "true"
spec:
  selector:
    app: autograntpro-manus
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
