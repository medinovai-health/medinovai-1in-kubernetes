apiVersion: v1
kind: Service
metadata:
  name: cdr
  namespace: platform
  labels:
    app: cdr
    atlasos.io/managed: "true"
spec:
  selector:
    app: cdr
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
