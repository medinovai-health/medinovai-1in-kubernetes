apiVersion: v1
kind: Service
metadata:
  name: autograntprocursorstudio
  namespace: platform
  labels:
    app: autograntprocursorstudio
    atlasos.io/managed: "true"
spec:
  selector:
    app: autograntprocursorstudio
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
