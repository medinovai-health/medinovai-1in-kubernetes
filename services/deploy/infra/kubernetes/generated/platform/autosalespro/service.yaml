apiVersion: v1
kind: Service
metadata:
  name: autosalespro
  namespace: platform
  labels:
    app: autosalespro
    atlasos.io/managed: "true"
spec:
  selector:
    app: autosalespro
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
