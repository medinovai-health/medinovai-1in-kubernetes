apiVersion: v1
kind: Service
metadata:
  name: subscription
  namespace: platform
  labels:
    app: subscription
    atlasos.io/managed: "true"
spec:
  selector:
    app: subscription
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
