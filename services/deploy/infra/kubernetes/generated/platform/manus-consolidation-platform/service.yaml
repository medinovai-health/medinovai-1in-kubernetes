apiVersion: v1
kind: Service
metadata:
  name: manus-consolidation-platform
  namespace: platform
  labels:
    app: manus-consolidation-platform
    atlasos.io/managed: "true"
spec:
  selector:
    app: manus-consolidation-platform
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
