apiVersion: v1
kind: Service
metadata:
  name: mywork-by-myonsite-v2
  namespace: platform
  labels:
    app: mywork-by-myonsite-v2
    atlasos.io/managed: "true"
spec:
  selector:
    app: mywork-by-myonsite-v2
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
