apiVersion: v1
kind: Service
metadata:
  name: local-llms
  namespace: platform
  labels:
    app: local-llms
    atlasos.io/managed: "true"
spec:
  selector:
    app: local-llms
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
