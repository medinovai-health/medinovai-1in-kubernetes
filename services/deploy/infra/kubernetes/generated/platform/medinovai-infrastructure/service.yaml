apiVersion: v1
kind: Service
metadata:
  name: medinovai-infrastructure
  namespace: platform
  labels:
    app: medinovai-infrastructure
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-infrastructure
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
