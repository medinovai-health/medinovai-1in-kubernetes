apiVersion: v1
kind: Service
metadata:
  name: clawdbot-ui-fixes
  namespace: platform
  labels:
    app: clawdbot-ui-fixes
    atlasos.io/managed: "true"
spec:
  selector:
    app: clawdbot-ui-fixes
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
