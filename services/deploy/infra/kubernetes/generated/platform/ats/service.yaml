apiVersion: v1
kind: Service
metadata:
  name: ats
  namespace: platform
  labels:
    app: ats
    atlasos.io/managed: "true"
spec:
  selector:
    app: ats
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
