apiVersion: v1
kind: Service
metadata:
  name: docugenie
  namespace: platform
  labels:
    app: docugenie
    atlasos.io/managed: "true"
spec:
  selector:
    app: docugenie
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
