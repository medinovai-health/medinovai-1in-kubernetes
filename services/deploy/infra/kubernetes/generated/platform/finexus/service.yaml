apiVersion: v1
kind: Service
metadata:
  name: finexus
  namespace: platform
  labels:
    app: finexus
    atlasos.io/managed: "true"
spec:
  selector:
    app: finexus
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
