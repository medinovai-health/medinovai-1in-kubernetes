apiVersion: v1
kind: Service
metadata:
  name: medinovai-policy-diff-watcher
  namespace: platform
  labels:
    app: medinovai-policy-diff-watcher
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-policy-diff-watcher
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
