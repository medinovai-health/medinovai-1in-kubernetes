apiVersion: v1
kind: Service
metadata:
  name: myonsiteoperationsmonitoringv1
  namespace: platform
  labels:
    app: myonsiteoperationsmonitoringv1
    atlasos.io/managed: "true"
spec:
  selector:
    app: myonsiteoperationsmonitoringv1
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
