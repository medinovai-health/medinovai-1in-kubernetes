apiVersion: v1
kind: Service
metadata:
  name: medinovai-atlasos
  namespace: platform
  labels:
    app: medinovai-atlasos
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-atlasos
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
