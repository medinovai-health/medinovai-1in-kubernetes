apiVersion: v1
kind: Service
metadata:
  name: medinovai-api
  namespace: platform
  labels:
    app: medinovai-api
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-api
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
