apiVersion: v1
kind: Service
metadata:
  name: myhealthconcierge-ios
  namespace: platform
  labels:
    app: myhealthconcierge-ios
    atlasos.io/managed: "true"
spec:
  selector:
    app: myhealthconcierge-ios
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
