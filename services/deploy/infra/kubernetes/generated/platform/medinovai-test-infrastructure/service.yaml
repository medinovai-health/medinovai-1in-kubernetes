apiVersion: v1
kind: Service
metadata:
  name: medinovai-test-infrastructure
  namespace: platform
  labels:
    app: medinovai-test-infrastructure
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-test-infrastructure
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
