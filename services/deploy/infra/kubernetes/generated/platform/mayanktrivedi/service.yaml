apiVersion: v1
kind: Service
metadata:
  name: mayanktrivedi
  namespace: platform
  labels:
    app: mayanktrivedi
    atlasos.io/managed: "true"
spec:
  selector:
    app: mayanktrivedi
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
