apiVersion: v1
kind: Service
metadata:
  name: medinovai-saes
  namespace: platform
  labels:
    app: medinovai-saes
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-saes
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
