apiVersion: v1
kind: Service
metadata:
  name: reposleuth
  namespace: platform
  labels:
    app: reposleuth
    atlasos.io/managed: "true"
spec:
  selector:
    app: reposleuth
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
