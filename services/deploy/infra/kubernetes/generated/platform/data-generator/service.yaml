apiVersion: v1
kind: Service
metadata:
  name: data-generator
  namespace: platform
  labels:
    app: data-generator
    atlasos.io/managed: "true"
spec:
  selector:
    app: data-generator
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
