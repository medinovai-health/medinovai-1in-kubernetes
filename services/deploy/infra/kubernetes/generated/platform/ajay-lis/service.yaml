apiVersion: v1
kind: Service
metadata:
  name: ajay-lis
  namespace: platform
  labels:
    app: ajay-lis
    atlasos.io/managed: "true"
spec:
  selector:
    app: ajay-lis
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
