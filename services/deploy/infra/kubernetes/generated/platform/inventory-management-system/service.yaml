apiVersion: v1
kind: Service
metadata:
  name: inventory-management-system
  namespace: platform
  labels:
    app: inventory-management-system
    atlasos.io/managed: "true"
spec:
  selector:
    app: inventory-management-system
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
