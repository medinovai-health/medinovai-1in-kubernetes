apiVersion: v1
kind: Service
metadata:
  name: operationsanalytics
  namespace: platform
  labels:
    app: operationsanalytics
    atlasos.io/managed: "true"
spec:
  selector:
    app: operationsanalytics
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
