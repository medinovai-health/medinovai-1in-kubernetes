apiVersion: v1
kind: Service
metadata:
  name: neocognition
  namespace: platform
  labels:
    app: neocognition
    atlasos.io/managed: "true"
spec:
  selector:
    app: neocognition
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
