apiVersion: v1
kind: Service
metadata:
  name: cogniai-us
  namespace: platform
  labels:
    app: cogniai-us
    atlasos.io/managed: "true"
spec:
  selector:
    app: cogniai-us
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
