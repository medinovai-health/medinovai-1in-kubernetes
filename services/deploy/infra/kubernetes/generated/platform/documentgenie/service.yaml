apiVersion: v1
kind: Service
metadata:
  name: documentgenie
  namespace: platform
  labels:
    app: documentgenie
    atlasos.io/managed: "true"
spec:
  selector:
    app: documentgenie
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
