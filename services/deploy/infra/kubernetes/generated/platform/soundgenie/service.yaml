apiVersion: v1
kind: Service
metadata:
  name: soundgenie
  namespace: platform
  labels:
    app: soundgenie
    atlasos.io/managed: "true"
spec:
  selector:
    app: soundgenie
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
