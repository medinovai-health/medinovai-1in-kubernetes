apiVersion: v1
kind: Service
metadata:
  name: automarketingpro
  namespace: platform
  labels:
    app: automarketingpro
    atlasos.io/managed: "true"
spec:
  selector:
    app: automarketingpro
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
