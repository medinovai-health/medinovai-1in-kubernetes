apiVersion: v1
kind: Service
metadata:
  name: deployment
  namespace: platform
  labels:
    app: deployment
    atlasos.io/managed: "true"
spec:
  selector:
    app: deployment
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
