apiVersion: v1
kind: Service
metadata:
  name: medinovai-deploy
  namespace: platform
  labels:
    app: medinovai-deploy
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-deploy
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
