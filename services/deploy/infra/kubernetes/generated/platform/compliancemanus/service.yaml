apiVersion: v1
kind: Service
metadata:
  name: compliancemanus
  namespace: platform
  labels:
    app: compliancemanus
    atlasos.io/managed: "true"
spec:
  selector:
    app: compliancemanus
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
