apiVersion: v1
kind: Service
metadata:
  name: medinovai-email-service
  namespace: platform
  labels:
    app: medinovai-email-service
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-email-service
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
