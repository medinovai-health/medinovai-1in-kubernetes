apiVersion: v1
kind: Service
metadata:
  name: medinovaiworkflow
  namespace: platform
  labels:
    app: medinovaiworkflow
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovaiworkflow
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
