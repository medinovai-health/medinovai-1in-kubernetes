apiVersion: v1
kind: Service
metadata:
  name: localmodels
  namespace: platform
  labels:
    app: localmodels
    atlasos.io/managed: "true"
spec:
  selector:
    app: localmodels
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
