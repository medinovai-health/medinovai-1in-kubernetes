apiVersion: v1
kind: Service
metadata:
  name: medinovai-clinical-decision-support
  namespace: clinical
  labels:
    app: medinovai-clinical-decision-support
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-clinical-decision-support
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
