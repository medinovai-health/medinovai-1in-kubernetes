apiVersion: v1
kind: Service
metadata:
  name: medinovai-wait-list-balancer
  namespace: clinical
  labels:
    app: medinovai-wait-list-balancer
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-wait-list-balancer
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
