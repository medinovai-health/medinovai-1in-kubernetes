apiVersion: v1
kind: Service
metadata:
  name: medinovai-etmf
  namespace: clinical
  labels:
    app: medinovai-etmf
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-etmf
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
