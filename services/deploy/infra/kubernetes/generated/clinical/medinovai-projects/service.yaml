apiVersion: v1
kind: Service
metadata:
  name: medinovai-projects
  namespace: clinical
  labels:
    app: medinovai-projects
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-projects
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
