apiVersion: v1
kind: Service
metadata:
  name: medinovai-inventorymanagement
  namespace: clinical
  labels:
    app: medinovai-inventorymanagement
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-inventorymanagement
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
