apiVersion: v1
kind: Service
metadata:
  name: medinovai-patientmatching
  namespace: clinical
  labels:
    app: medinovai-patientmatching
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-patientmatching
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
