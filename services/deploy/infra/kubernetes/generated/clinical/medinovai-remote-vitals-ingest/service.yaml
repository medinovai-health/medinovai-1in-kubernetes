apiVersion: v1
kind: Service
metadata:
  name: medinovai-remote-vitals-ingest
  namespace: clinical
  labels:
    app: medinovai-remote-vitals-ingest
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-remote-vitals-ingest
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
