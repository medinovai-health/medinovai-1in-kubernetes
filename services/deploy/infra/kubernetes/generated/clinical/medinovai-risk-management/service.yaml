apiVersion: v1
kind: Service
metadata:
  name: medinovai-risk-management
  namespace: clinical
  labels:
    app: medinovai-risk-management
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-risk-management
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
