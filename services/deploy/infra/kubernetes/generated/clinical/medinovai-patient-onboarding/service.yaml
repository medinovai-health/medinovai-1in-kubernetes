apiVersion: v1
kind: Service
metadata:
  name: medinovai-patient-onboarding
  namespace: clinical
  labels:
    app: medinovai-patient-onboarding
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-patient-onboarding
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
