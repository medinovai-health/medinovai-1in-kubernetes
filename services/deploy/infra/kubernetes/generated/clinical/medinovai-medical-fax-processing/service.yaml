apiVersion: v1
kind: Service
metadata:
  name: medinovai-medical-fax-processing
  namespace: clinical
  labels:
    app: medinovai-medical-fax-processing
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-medical-fax-processing
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
