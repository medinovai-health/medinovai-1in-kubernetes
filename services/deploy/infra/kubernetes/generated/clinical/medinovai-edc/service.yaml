apiVersion: v1
kind: Service
metadata:
  name: medinovai-edc
  namespace: clinical
  labels:
    app: medinovai-edc
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-edc
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
