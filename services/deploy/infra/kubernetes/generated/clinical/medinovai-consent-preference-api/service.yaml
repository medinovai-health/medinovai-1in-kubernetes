apiVersion: v1
kind: Service
metadata:
  name: medinovai-consent-preference-api
  namespace: clinical
  labels:
    app: medinovai-consent-preference-api
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-consent-preference-api
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
