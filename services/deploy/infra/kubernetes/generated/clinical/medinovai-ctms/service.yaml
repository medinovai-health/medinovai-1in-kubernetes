apiVersion: v1
kind: Service
metadata:
  name: medinovai-ctms
  namespace: clinical
  labels:
    app: medinovai-ctms
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-ctms
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
