apiVersion: v1
kind: Service
metadata:
  name: medinovai-imaging-viewer
  namespace: clinical
  labels:
    app: medinovai-imaging-viewer
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-imaging-viewer
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
