apiVersion: v1
kind: Service
metadata:
  name: medinovai-health-timeline
  namespace: clinical
  labels:
    app: medinovai-health-timeline
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-health-timeline
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
