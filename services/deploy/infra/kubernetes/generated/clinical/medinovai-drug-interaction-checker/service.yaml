apiVersion: v1
kind: Service
metadata:
  name: medinovai-drug-interaction-checker
  namespace: clinical
  labels:
    app: medinovai-drug-interaction-checker
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-drug-interaction-checker
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
