apiVersion: v1
kind: Service
metadata:
  name: medinovai-care-team-chat
  namespace: clinical
  labels:
    app: medinovai-care-team-chat
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-care-team-chat
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
