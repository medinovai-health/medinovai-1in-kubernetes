apiVersion: v1
kind: Service
metadata:
  name: medinovai-virtual-triage
  namespace: clinical
  labels:
    app: medinovai-virtual-triage
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-virtual-triage
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
