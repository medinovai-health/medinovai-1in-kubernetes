apiVersion: v1
kind: Service
metadata:
  name: medinovai-eisf
  namespace: clinical
  labels:
    app: medinovai-eisf
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-eisf
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
