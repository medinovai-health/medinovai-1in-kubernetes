apiVersion: v1
kind: Service
metadata:
  name: medinovai-esource
  namespace: clinical
  labels:
    app: medinovai-esource
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-esource
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
