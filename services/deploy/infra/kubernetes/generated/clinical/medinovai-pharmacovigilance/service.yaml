apiVersion: v1
kind: Service
metadata:
  name: medinovai-pharmacovigilance
  namespace: clinical
  labels:
    app: medinovai-pharmacovigilance
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-pharmacovigilance
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
