apiVersion: v1
kind: Service
metadata:
  name: medinovai-medication-tracker
  namespace: clinical
  labels:
    app: medinovai-medication-tracker
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-medication-tracker
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
