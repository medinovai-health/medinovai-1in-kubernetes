apiVersion: v1
kind: Service
metadata:
  name: medinovai-cds
  namespace: clinical
  labels:
    app: medinovai-cds
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-cds
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
