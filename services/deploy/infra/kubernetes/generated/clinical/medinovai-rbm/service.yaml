apiVersion: v1
kind: Service
metadata:
  name: medinovai-rbm
  namespace: clinical
  labels:
    app: medinovai-rbm
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-rbm
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
