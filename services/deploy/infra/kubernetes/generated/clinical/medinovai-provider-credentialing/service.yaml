apiVersion: v1
kind: Service
metadata:
  name: medinovai-provider-credentialing
  namespace: clinical
  labels:
    app: medinovai-provider-credentialing
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-provider-credentialing
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
