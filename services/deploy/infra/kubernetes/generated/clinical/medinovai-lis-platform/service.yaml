apiVersion: v1
kind: Service
metadata:
  name: medinovai-lis-platform
  namespace: clinical
  labels:
    app: medinovai-lis-platform
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-lis-platform
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
