apiVersion: v1
kind: Service
metadata:
  name: medinovai-sitefeasibility
  namespace: clinical
  labels:
    app: medinovai-sitefeasibility
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-sitefeasibility
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
