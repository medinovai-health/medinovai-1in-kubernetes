apiVersion: v1
kind: Service
metadata:
  name: medinovai-help
  namespace: frontend
  labels:
    app: medinovai-help
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-help
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
