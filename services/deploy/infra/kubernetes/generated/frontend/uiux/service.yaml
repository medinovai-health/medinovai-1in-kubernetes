apiVersion: v1
kind: Service
metadata:
  name: uiux
  namespace: frontend
  labels:
    app: uiux
    atlasos.io/managed: "true"
spec:
  selector:
    app: uiux
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
