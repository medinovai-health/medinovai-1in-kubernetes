apiVersion: v1
kind: Service
metadata:
  name: medinovai-task-kanban
  namespace: frontend
  labels:
    app: medinovai-task-kanban
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-task-kanban
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
