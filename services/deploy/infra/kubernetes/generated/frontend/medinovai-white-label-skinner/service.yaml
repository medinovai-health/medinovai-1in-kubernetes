apiVersion: v1
kind: Service
metadata:
  name: medinovai-white-label-skinner
  namespace: frontend
  labels:
    app: medinovai-white-label-skinner
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-white-label-skinner
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
