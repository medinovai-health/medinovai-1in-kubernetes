apiVersion: v1
kind: Service
metadata:
  name: vibe-kanban
  namespace: frontend
  labels:
    app: vibe-kanban
    atlasos.io/managed: "true"
spec:
  selector:
    app: vibe-kanban
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
