apiVersion: v1
kind: Service
metadata:
  name: medinovai-uiux
  namespace: frontend
  labels:
    app: medinovai-uiux
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-uiux
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
