apiVersion: apps/v1
kind: Deployment
metadata:
  name: medinovai-notification-center
  namespace: medinovai-services
  labels:
    app: medinovai-notification-center
    tier: platform
    app.kubernetes.io/managed-by: medinovai-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: medinovai-notification-center
  template:
    metadata:
      labels:
        app: medinovai-notification-center
        tier: platform
        app.kubernetes.io/managed-by: medinovai-deploy
    spec:
      containers:
        - name: medinovai-notification-center
          image: medinovai-notification-center:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: SERVICE_NAME
              value: medinovai-notification-center
            - name: PORT
              value: "8080"
            - name: REGISTRY_URL
              value: http://medinovai-registry.medinovai-services:8080
            - name: DATABASE_URL
              value: postgresql://medinovai:medinovai_dev@postgres-primary.medinovai-data:5432/medinovai_main
            - name: REDIS_URL
              value: redis://:medinovai_dev@redis.medinovai-data:6379
            - name: SECURITY_SERVICE_URL
              value: http://medinovai-security.medinovai-security:9000
            - name: RABBITMQ_URL
              value: amqp://medinovai:medinovai_dev@rabbitmq.medinovai-data:5672
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 2
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 45
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: medinovai-notification-center
  namespace: medinovai-services
  labels:
    app: medinovai-notification-center
    tier: platform
    app.kubernetes.io/managed-by: medinovai-deploy
spec:
  selector:
    app: medinovai-notification-center
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
