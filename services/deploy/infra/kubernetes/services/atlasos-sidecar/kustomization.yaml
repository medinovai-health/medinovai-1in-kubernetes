apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: atlasos

resources:
  - configmap.yaml
  - mutating-webhook.yaml
  - webhook-deployment.yaml
  - service.yaml
  - certificate.yaml
