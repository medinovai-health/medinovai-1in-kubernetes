apiVersion: v1
kind: Service
metadata:
  name: lobster-engine
  namespace: medinovai
  labels:
    app.kubernetes.io/name: lobster-engine
    app.kubernetes.io/part-of: atlasos
spec:
  type: ClusterIP
  ports:
    - port: 3116
      targetPort: 3116
      protocol: TCP
      name: http
  selector:
    app: lobster-engine
