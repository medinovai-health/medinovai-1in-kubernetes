apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-config-api
  namespace: medinovai
  labels:
    app: agent-config-api
    app.kubernetes.io/name: agent-config-api
    app.kubernetes.io/part-of: atlasos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-config-api
  template:
    metadata:
      labels:
        app: agent-config-api
    spec:
      containers:
        - name: agent-config-api
          image: atlasos-agent-config-api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3112
          env:
            - name: REGISTRY_URL
              value: "http://medinovai-registry.medinovai:8000"
            - name: SERVICE_NAME
              value: "agent-config-api"
            - name: PORT
              value: "3112"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3112
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 2
          livenessProbe:
            httpGet:
              path: /health
              port: 3112
            initialDelaySeconds: 45
            periodSeconds: 15
            failureThreshold: 3
