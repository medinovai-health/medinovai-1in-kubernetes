apiVersion: v1
kind: Service
metadata:
  name: agent-config-api
  namespace: medinovai
  labels:
    app.kubernetes.io/name: agent-config-api
    app.kubernetes.io/part-of: atlasos
spec:
  type: ClusterIP
  ports:
    - port: 3112
      targetPort: 3112
      protocol: TCP
      name: http
  selector:
    app: agent-config-api
