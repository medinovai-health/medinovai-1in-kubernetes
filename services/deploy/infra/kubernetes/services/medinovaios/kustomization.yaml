apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# medinovaiOS — Unified portal for all MedinovAI products and infrastructure
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
