apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: medinovai-services
spec:
  template:
    spec:
      securityContext:
        runAsNonRoot: false
      containers:
        - name: auth-service
          image: traefik/whoami:latest
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health
              port: 80
            failureThreshold: 10
            periodSeconds: 3
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          envFrom:
            - configMapRef:
                name: medinovai-env
