apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: medinovai-services
spec:
  template:
    spec:
      containers:
        - name: api-gateway
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
