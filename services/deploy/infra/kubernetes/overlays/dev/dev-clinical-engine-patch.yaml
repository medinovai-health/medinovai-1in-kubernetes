apiVersion: apps/v1
kind: Deployment
metadata:
  name: clinical-engine
  namespace: medinovai-services
spec:
  template:
    spec:
      containers:
        - name: clinical-engine
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
