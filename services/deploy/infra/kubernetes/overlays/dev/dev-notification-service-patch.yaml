apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: medinovai-services
spec:
  template:
    spec:
      containers:
        - name: notification-service
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
