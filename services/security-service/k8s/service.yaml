apiVersion: v1
kind: Service
metadata:
  name: medinovai-security-service
  namespace: medinovai-services
  labels:
    app: medinovai-security-service
    tier: platform
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: medinovai-security-service
