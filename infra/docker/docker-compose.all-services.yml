# Auto-generated by scripts/deploy/generate_all_services_compose.py
# DO NOT EDIT — regenerate with: make generate-all-services
# Services: 95
# Source: config/dependency-graph.json v2.0.0

services:
  medinovai-secrets-manager-bridge:
    image: medinovai-secrets-manager-bridge:latest
    pull_policy: never
    container_name: medinovai-secrets-manager-bridge
    ports:
      - "10001:8000"
    environment:
      SERVICE_NAME: "medinovai-secrets-manager-bridge"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-security:
    image: medinovai-security:latest
    pull_policy: never
    container_name: medinovai-security
    ports:
      - "10002:9000"
    environment:
      SERVICE_NAME: "medinovai-security"
      PORT: "9000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-universal-sign-on:
    image: medinovai-universal-sign-on:latest
    pull_policy: never
    container_name: medinovai-universal-sign-on
    ports:
      - "10003:8000"
    environment:
      SERVICE_NAME: "medinovai-universal-sign-on"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-role-based-permissions:
    image: medinovai-role-based-permissions:latest
    pull_policy: never
    container_name: medinovai-role-based-permissions
    ports:
      - "10004:8000"
    environment:
      SERVICE_NAME: "medinovai-role-based-permissions"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-encryption-vault:
    image: medinovai-encryption-vault:latest
    pull_policy: never
    container_name: medinovai-encryption-vault
    ports:
      - "10005:8000"
    environment:
      SERVICE_NAME: "medinovai-encryption-vault"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-hipaa-gdpr-guard:
    image: medinovai-hipaa-gdpr-guard:latest
    pull_policy: never
    container_name: medinovai-hipaa-gdpr-guard
    ports:
      - "10006:8000"
    environment:
      SERVICE_NAME: "medinovai-hipaa-gdpr-guard"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-consent-preference-api:
    image: medinovai-consent-preference-api:latest
    pull_policy: never
    container_name: medinovai-consent-preference-api
    ports:
      - "10007:8000"
    environment:
      SERVICE_NAME: "medinovai-consent-preference-api"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-audit-trail-explorer:
    image: medinovai-audit-trail-explorer:latest
    pull_policy: never
    container_name: medinovai-audit-trail-explorer
    ports:
      - "10008:8000"
    environment:
      SERVICE_NAME: "medinovai-audit-trail-explorer"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-registry:
    image: medinovai-registry:latest
    pull_policy: never
    container_name: medinovai-registry
    ports:
      - "10100:8080"
    environment:
      SERVICE_NAME: "medinovai-registry"
      PORT: "8080"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-data-services:
    image: medinovai-data-services:latest
    pull_policy: never
    container_name: medinovai-data-services
    ports:
      - "10101:8000"
    environment:
      SERVICE_NAME: "medinovai-data-services"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-real-time-stream-bus:
    image: medinovai-real-time-stream-bus:latest
    pull_policy: never
    container_name: medinovai-real-time-stream-bus
    ports:
      - "10102:3000"
    environment:
      SERVICE_NAME: "medinovai-real-time-stream-bus"
      PORT: "3000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3000/health/ready || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-configuration-management:
    image: medinovai-configuration-management:latest
    pull_policy: never
    container_name: medinovai-configuration-management
    ports:
      - "10103:8000"
    environment:
      SERVICE_NAME: "medinovai-configuration-management"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-notification-center:
    image: medinovai-notification-center:latest
    pull_policy: never
    container_name: medinovai-notification-center
    ports:
      - "10104:8080"
    environment:
      SERVICE_NAME: "medinovai-notification-center"
      PORT: "8080"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-api-gateway:
    image: medinovai-api-gateway:latest
    pull_policy: never
    container_name: medinovai-api-gateway
    ports:
      - "10105:8080"
    environment:
      SERVICE_NAME: "medinovai-api-gateway"
      PORT: "8080"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-atlas-engine:
    image: medinovai-atlas-engine:latest
    pull_policy: never
    container_name: medinovai-atlas-engine
    ports:
      - "10106:8000"
    environment:
      SERVICE_NAME: "medinovai-atlas-engine"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-healthllm:
    image: medinovai-healthllm:latest
    pull_policy: never
    container_name: medinovai-healthllm
    ports:
      - "10200:8000"
    environment:
      SERVICE_NAME: "medinovai-healthLLM"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-model-service-orchestrator:
    image: medinovai-model-service-orchestrator:latest
    pull_policy: never
    container_name: medinovai-model-service-orchestrator
    ports:
      - "10201:8000"
    environment:
      SERVICE_NAME: "medinovai-model-service-orchestrator"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-knowledge-graph:
    image: medinovai-knowledge-graph:latest
    pull_policy: never
    container_name: medinovai-knowledge-graph
    ports:
      - "10202:8000"
    environment:
      SERVICE_NAME: "medinovai-knowledge-graph"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-clinical-decision-support:
    image: medinovai-clinical-decision-support:latest
    pull_policy: never
    container_name: medinovai-clinical-decision-support
    ports:
      - "10203:8000"
    environment:
      SERVICE_NAME: "medinovai-clinical-decision-support"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-patient-services:
    image: medinovai-patient-services:latest
    pull_policy: never
    container_name: medinovai-patient-services
    ports:
      - "10204:8000"
    environment:
      SERVICE_NAME: "medinovai-patient-services"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-patient-onboarding:
    image: medinovai-patient-onboarding:latest
    pull_policy: never
    container_name: medinovai-patient-onboarding
    ports:
      - "10300:8000"
    environment:
      SERVICE_NAME: "medinovai-patient-onboarding"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-patientmatching:
    image: medinovai-patientmatching:latest
    pull_policy: never
    container_name: medinovai-patientmatching
    ports:
      - "10301:8080"
    environment:
      SERVICE_NAME: "medinovai-patientmatching"
      PORT: "8080"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-health-timeline:
    image: medinovai-health-timeline:latest
    pull_policy: never
    container_name: medinovai-health-timeline
    ports:
      - "10302:8000"
    environment:
      SERVICE_NAME: "medinovai-health-timeline"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-care-team-chat:
    image: medinovai-care-team-chat:latest
    pull_policy: never
    container_name: medinovai-care-team-chat
    ports:
      - "10303:8000"
    environment:
      SERVICE_NAME: "medinovai-care-team-chat"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-smart-scheduler:
    image: medinovai-smart-scheduler:latest
    pull_policy: never
    container_name: medinovai-smart-scheduler
    ports:
      - "10304:8000"
    environment:
      SERVICE_NAME: "medinovai-smart-scheduler"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-wait-list-balancer:
    image: medinovai-wait-list-balancer:latest
    pull_policy: never
    container_name: medinovai-wait-list-balancer
    ports:
      - "10305:8080"
    environment:
      SERVICE_NAME: "medinovai-wait-list-balancer"
      PORT: "8080"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-virtual-triage:
    image: medinovai-virtual-triage:latest
    pull_policy: never
    container_name: medinovai-virtual-triage
    ports:
      - "10306:8000"
    environment:
      SERVICE_NAME: "medinovai-virtual-triage"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-telehealth-hub:
    image: medinovai-telehealth-hub:latest
    pull_policy: never
    container_name: medinovai-telehealth-hub
    ports:
      - "10307:8080"
    environment:
      SERVICE_NAME: "medinovai-telehealth-hub"
      PORT: "8080"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-remote-vitals-ingest:
    image: medinovai-remote-vitals-ingest:latest
    pull_policy: never
    container_name: medinovai-remote-vitals-ingest
    ports:
      - "10308:8000"
    environment:
      SERVICE_NAME: "medinovai-remote-vitals-ingest"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-lab-order-router:
    image: medinovai-lab-order-router:latest
    pull_policy: never
    container_name: medinovai-lab-order-router
    ports:
      - "10309:8000"
    environment:
      SERVICE_NAME: "medinovai-lab-order-router"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-pathology-ai:
    image: medinovai-pathology-ai:latest
    pull_policy: never
    container_name: medinovai-pathology-ai
    ports:
      - "10310:8000"
    environment:
      SERVICE_NAME: "medinovai-pathology-ai"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-imaging-viewer:
    image: medinovai-imaging-viewer:latest
    pull_policy: never
    container_name: medinovai-imaging-viewer
    ports:
      - "10311:8000"
    environment:
      SERVICE_NAME: "medinovai-imaging-viewer"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-genomics-interpreter:
    image: medinovai-genomics-interpreter:latest
    pull_policy: never
    container_name: medinovai-genomics-interpreter
    ports:
      - "10312:8000"
    environment:
      SERVICE_NAME: "medinovai-genomics-interpreter"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-image-to-text-ocr:
    image: medinovai-image-to-text-ocr:latest
    pull_policy: never
    container_name: medinovai-image-to-text-ocr
    ports:
      - "10313:8000"
    environment:
      SERVICE_NAME: "medinovai-image-to-text-ocr"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-chatbot:
    image: medinovai-chatbot:latest
    pull_policy: never
    container_name: medinovai-chatbot
    ports:
      - "10314:8000"
    environment:
      SERVICE_NAME: "medinovai-chatbot"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-ai-scribe:
    image: medinovai-ai-scribe:latest
    pull_policy: never
    container_name: medinovai-ai-scribe
    ports:
      - "10315:8000"
    environment:
      SERVICE_NAME: "medinovai-ai-scribe"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-doc-summarizer:
    image: medinovai-doc-summarizer:latest
    pull_policy: never
    container_name: medinovai-doc-summarizer
    ports:
      - "10316:8000"
    environment:
      SERVICE_NAME: "medinovai-doc-summarizer"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-natural-language-query:
    image: medinovai-natural-language-query:latest
    pull_policy: never
    container_name: medinovai-natural-language-query
    ports:
      - "10317:8000"
    environment:
      SERVICE_NAME: "medinovai-natural-language-query"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-anomaly-detector:
    image: medinovai-anomaly-detector:latest
    pull_policy: never
    container_name: medinovai-anomaly-detector
    ports:
      - "10318:8000"
    environment:
      SERVICE_NAME: "medinovai-anomaly-detector"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-sentiment-monitor:
    image: medinovai-sentiment-monitor:latest
    pull_policy: never
    container_name: medinovai-sentiment-monitor
    ports:
      - "10319:8000"
    environment:
      SERVICE_NAME: "medinovai-sentiment-monitor"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-drug-interaction-checker:
    image: medinovai-drug-interaction-checker:latest
    pull_policy: never
    container_name: medinovai-drug-interaction-checker
    ports:
      - "10320:8000"
    environment:
      SERVICE_NAME: "medinovai-drug-interaction-checker"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-medical-fax-processing:
    image: medinovai-medical-fax-processing:latest
    pull_policy: never
    container_name: medinovai-medical-fax-processing
    ports:
      - "10321:8000"
    environment:
      SERVICE_NAME: "medinovai-medical-fax-processing"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-content-translator:
    image: medinovai-content-translator:latest
    pull_policy: never
    container_name: medinovai-content-translator
    ports:
      - "10322:8000"
    environment:
      SERVICE_NAME: "medinovai-content-translator"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-text-to-speech-narrator:
    image: medinovai-text-to-speech-narrator:latest
    pull_policy: never
    container_name: medinovai-text-to-speech-narrator
    ports:
      - "10323:8000"
    environment:
      SERVICE_NAME: "medinovai-text-to-speech-narrator"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-voice-command-layer:
    image: medinovai-voice-command-layer:latest
    pull_policy: never
    container_name: medinovai-voice-command-layer
    ports:
      - "10324:8000"
    environment:
      SERVICE_NAME: "medinovai-voice-command-layer"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.0'
        reservations:
          memory: 256m
          cpus: '0.2'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-e-prescribe-gateway:
    image: medinovai-e-prescribe-gateway:latest
    pull_policy: never
    container_name: medinovai-e-prescribe-gateway
    ports:
      - "10325:8000"
    environment:
      SERVICE_NAME: "medinovai-e-prescribe-gateway"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-medication-tracker:
    image: medinovai-medication-tracker:latest
    pull_policy: never
    container_name: medinovai-medication-tracker
    ports:
      - "10326:8000"
    environment:
      SERVICE_NAME: "medinovai-medication-tracker"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-ctms:
    image: medinovai-ctms:latest
    pull_policy: never
    container_name: medinovai-ctms
    ports:
      - "10327:8000"
    environment:
      SERVICE_NAME: "medinovai-CTMS"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-edc:
    image: medinovai-edc:latest
    pull_policy: never
    container_name: medinovai-edc
    ports:
      - "10328:8000"
    environment:
      SERVICE_NAME: "medinovai-EDC"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-etmf:
    image: medinovai-etmf:latest
    pull_policy: never
    container_name: medinovai-etmf
    ports:
      - "10329:8000"
    environment:
      SERVICE_NAME: "medinovai-etmf"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-saes:
    image: medinovai-saes:latest
    pull_policy: never
    container_name: medinovai-saes
    ports:
      - "10330:8080"
    environment:
      SERVICE_NAME: "medinovai-saes"
      PORT: "8080"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-econsent:
    image: medinovai-econsent:latest
    pull_policy: never
    container_name: medinovai-econsent
    ports:
      - "10331:8000"
    environment:
      SERVICE_NAME: "medinovai-eConsent"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-epro:
    image: medinovai-epro:latest
    pull_policy: never
    container_name: medinovai-epro
    ports:
      - "10332:8000"
    environment:
      SERVICE_NAME: "medinovai-ePRO"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-esource:
    image: medinovai-esource:latest
    pull_policy: never
    container_name: medinovai-esource
    ports:
      - "10333:8000"
    environment:
      SERVICE_NAME: "medinovai-eSource"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-eisf:
    image: medinovai-eisf:latest
    pull_policy: never
    container_name: medinovai-eisf
    ports:
      - "10334:8000"
    environment:
      SERVICE_NAME: "medinovai-eISF"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-iwrs:
    image: medinovai-iwrs:latest
    pull_policy: never
    container_name: medinovai-iwrs
    ports:
      - "10335:8000"
    environment:
      SERVICE_NAME: "medinovai-iwrs"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-pharmacovigilance:
    image: medinovai-pharmacovigilance:latest
    pull_policy: never
    container_name: medinovai-pharmacovigilance
    ports:
      - "10336:8000"
    environment:
      SERVICE_NAME: "medinovai-Pharmacovigilance"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-researchsuite:
    image: medinovai-researchsuite:latest
    pull_policy: never
    container_name: medinovai-researchsuite
    ports:
      - "10337:8000"
    environment:
      SERVICE_NAME: "medinovai-ResearchSuite"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-regulatory-submissions:
    image: medinovai-regulatory-submissions:latest
    pull_policy: never
    container_name: medinovai-regulatory-submissions
    ports:
      - "10338:8000"
    environment:
      SERVICE_NAME: "medinovai-regulatory-submissions"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-rbm:
    image: medinovai-rbm:latest
    pull_policy: never
    container_name: medinovai-rbm
    ports:
      - "10339:8000"
    environment:
      SERVICE_NAME: "medinovai-RBM"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-reseach-fabric:
    image: medinovai-reseach-fabric:latest
    pull_policy: never
    container_name: medinovai-reseach-fabric
    ports:
      - "10340:8000"
    environment:
      SERVICE_NAME: "medinovai-reseach-fabric"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-sitefeasibility:
    image: medinovai-sitefeasibility:latest
    pull_policy: never
    container_name: medinovai-sitefeasibility
    ports:
      - "10341:8000"
    environment:
      SERVICE_NAME: "medinovai-SiteFeasibility"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-billing:
    image: medinovai-billing:latest
    pull_policy: never
    container_name: medinovai-billing
    ports:
      - "10342:8000"
    environment:
      SERVICE_NAME: "medinovai-billing"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-provider-credentialing:
    image: medinovai-provider-credentialing:latest
    pull_policy: never
    container_name: medinovai-provider-credentialing
    ports:
      - "10343:8000"
    environment:
      SERVICE_NAME: "medinovai-provider-credentialing"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-credentialing:
    image: medinovai-credentialing:latest
    pull_policy: never
    container_name: medinovai-credentialing
    ports:
      - "10344:3000"
    environment:
      SERVICE_NAME: "medinovai-credentialing"
      PORT: "3000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-employee-portal:
    image: medinovai-employee-portal:latest
    pull_policy: never
    container_name: medinovai-employee-portal
    ports:
      - "10345:3000"
    environment:
      SERVICE_NAME: "medinovai-employee-portal"
      PORT: "3000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-subscription:
    image: medinovai-subscription:latest
    pull_policy: never
    container_name: medinovai-subscription
    ports:
      - "10346:8000"
    environment:
      SERVICE_NAME: "medinovai-subscription"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-quality-certification:
    image: medinovai-quality-certification:latest
    pull_policy: never
    container_name: medinovai-quality-certification
    ports:
      - "10347:8000"
    environment:
      SERVICE_NAME: "medinovai-quality-certification"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-inventorymanagement:
    image: medinovai-inventorymanagement:latest
    pull_policy: never
    container_name: medinovai-inventorymanagement
    ports:
      - "10348:8000"
    environment:
      SERVICE_NAME: "medinovai-inventorymanagement"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-mail:
    image: medinovai-mail:latest
    pull_policy: never
    container_name: medinovai-mail
    ports:
      - "10349:8000"
    environment:
      SERVICE_NAME: "medinovai-mail"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-email-service:
    image: medinovai-email-service:latest
    pull_policy: never
    container_name: medinovai-email-service
    ports:
      - "10350:8000"
    environment:
      SERVICE_NAME: "medinovai-email-service"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-lis:
    image: medinovai-lis:latest
    pull_policy: never
    container_name: medinovai-lis
    ports:
      - "10351:8000"
    environment:
      SERVICE_NAME: "medinovai-lis"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-lis-platform:
    image: medinovai-lis-platform:latest
    pull_policy: never
    container_name: medinovai-lis-platform
    ports:
      - "10352:8000"
    environment:
      SERVICE_NAME: "medinovai-lis-platform"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-lis-ui:
    image: medinovai-lis-ui:latest
    pull_policy: never
    container_name: medinovai-lis-ui
    ports:
      - "10353:3000"
    environment:
      SERVICE_NAME: "medinovai-lis-ui"
      PORT: "3000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-edge-cache-cdn:
    image: medinovai-edge-cache-cdn:latest
    pull_policy: never
    container_name: medinovai-edge-cache-cdn
    ports:
      - "10400:8000"
    environment:
      SERVICE_NAME: "medinovai-edge-cache-cdn"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-data-lake-loader:
    image: medinovai-data-lake-loader:latest
    pull_policy: never
    container_name: medinovai-data-lake-loader
    ports:
      - "10401:8000"
    environment:
      SERVICE_NAME: "medinovai-data-lake-loader"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-feature-flag-console:
    image: medinovai-feature-flag-console:latest
    pull_policy: never
    container_name: medinovai-feature-flag-console
    ports:
      - "10402:8000"
    environment:
      SERVICE_NAME: "medinovai-feature-flag-console"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-canary-rollout-orchestrator:
    image: medinovai-canary-rollout-orchestrator:latest
    pull_policy: never
    container_name: medinovai-canary-rollout-orchestrator
    ports:
      - "10403:8000"
    environment:
      SERVICE_NAME: "medinovai-canary-rollout-orchestrator"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-devops-telemetry:
    image: medinovai-devops-telemetry:latest
    pull_policy: never
    container_name: medinovai-devops-telemetry
    ports:
      - "10404:8000"
    environment:
      SERVICE_NAME: "medinovai-devops-telemetry"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-policy-diff-watcher:
    image: medinovai-policy-diff-watcher:latest
    pull_policy: never
    container_name: medinovai-policy-diff-watcher
    ports:
      - "10405:8000"
    environment:
      SERVICE_NAME: "medinovai-policy-diff-watcher"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-etl-designer:
    image: medinovai-etl-designer:latest
    pull_policy: never
    container_name: medinovai-etl-designer
    ports:
      - "10406:8000"
    environment:
      SERVICE_NAME: "medinovai-etl-designer"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-prompt-vault:
    image: medinovai-prompt-vault:latest
    pull_policy: never
    container_name: medinovai-prompt-vault
    ports:
      - "10407:8000"
    environment:
      SERVICE_NAME: "medinovai-prompt-vault"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-qa-agent-builder:
    image: medinovai-qa-agent-builder:latest
    pull_policy: never
    container_name: medinovai-qa-agent-builder
    ports:
      - "10408:8000"
    environment:
      SERVICE_NAME: "medinovai-qa-agent-builder"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-task-kanban:
    image: medinovai-task-kanban:latest
    pull_policy: never
    container_name: medinovai-task-kanban
    ports:
      - "10409:8000"
    environment:
      SERVICE_NAME: "medinovai-task-kanban"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-guideline-updater:
    image: medinovai-guideline-updater:latest
    pull_policy: never
    container_name: medinovai-guideline-updater
    ports:
      - "10410:8000"
    environment:
      SERVICE_NAME: "medinovai-guideline-updater"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-white-label-skinner:
    image: medinovai-white-label-skinner:latest
    pull_policy: never
    container_name: medinovai-white-label-skinner
    ports:
      - "10411:8000"
    environment:
      SERVICE_NAME: "medinovai-white-label-skinner"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-accessibility-checker:
    image: medinovai-accessibility-checker:latest
    pull_policy: never
    container_name: medinovai-accessibility-checker
    ports:
      - "10412:8000"
    environment:
      SERVICE_NAME: "medinovai-accessibility-checker"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-governance-templates:
    image: medinovai-governance-templates:latest
    pull_policy: never
    container_name: medinovai-governance-templates
    ports:
      - "10413:8000"
    environment:
      SERVICE_NAME: "medinovai-governance-templates"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-risk-management:
    image: medinovai-risk-management:latest
    pull_policy: never
    container_name: medinovai-risk-management
    ports:
      - "10414:8000"
    environment:
      SERVICE_NAME: "medinovai-risk-management"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-cds:
    image: medinovai-cds:latest
    pull_policy: never
    container_name: medinovai-cds
    ports:
      - "10415:8000"
    environment:
      SERVICE_NAME: "medinovai-cds"
      PORT: "8000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-developer-portal:
    image: medinovai-developer-portal:latest
    pull_policy: never
    container_name: medinovai-developer-portal
    ports:
      - "10416:3000"
    environment:
      SERVICE_NAME: "medinovai-developer-portal"
      PORT: "3000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-livekit:
    image: medinovai-livekit:latest
    pull_policy: never
    container_name: medinovai-livekit
    ports:
      - "10417:7880"
    environment:
      SERVICE_NAME: "medinovai-Livekit"
      PORT: "7880"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:7880/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

  medinovai-multimodal-ui-shell:
    image: medinovai-multimodal-ui-shell:latest
    pull_policy: never
    container_name: medinovai-multimodal-ui-shell
    ports:
      - "10500:3000"
    environment:
      SERVICE_NAME: "medinovai-multimodal-ui-shell"
      PORT: "3000"
      REGISTRY_URL: "http://medinovai-registry:8080"
      VAULT_ADDR: "http://medinovai-vault:8200"
      VAULT_TOKEN: "${VAULT_DEV_ROOT_TOKEN:-medinovai-dev-token}"
      DATABASE_URL: "postgresql://medinovai:${POSTGRES_PASSWORD:-localdev}@medinovai-postgres:5432/medinovai_main"
      REDIS_URL: "redis://:${REDIS_PASSWORD:-localdev}@medinovai-redis:6379"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.5'
        reservations:
          memory: 64m
          cpus: '0.05'
    restart: unless-stopped
    networks:
      - medinovai-platform

networks:
  medinovai-platform:
    external: true
    name: medinovai-dev_medinovai-dev
