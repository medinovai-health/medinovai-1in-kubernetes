apiVersion: v1
kind: Service
metadata:
  name: medinovai-hipaa-gdpr-guard
  namespace: security
  labels:
    app: medinovai-hipaa-gdpr-guard
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-hipaa-gdpr-guard
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  type: ClusterIP
