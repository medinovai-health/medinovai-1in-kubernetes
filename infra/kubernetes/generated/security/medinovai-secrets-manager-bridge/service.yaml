apiVersion: v1
kind: Service
metadata:
  name: medinovai-secrets-manager-bridge
  namespace: security
  labels:
    app: medinovai-secrets-manager-bridge
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-secrets-manager-bridge
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  type: ClusterIP
