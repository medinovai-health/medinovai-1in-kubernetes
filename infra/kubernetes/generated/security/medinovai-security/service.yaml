apiVersion: v1
kind: Service
metadata:
  name: medinovai-security
  namespace: security
  labels:
    app: medinovai-security
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-security
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  type: ClusterIP
