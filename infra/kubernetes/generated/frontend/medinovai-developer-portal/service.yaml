apiVersion: v1
kind: Service
metadata:
  name: medinovai-developer-portal
  namespace: frontend
  labels:
    app: medinovai-developer-portal
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-developer-portal
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
