apiVersion: v1
kind: Service
metadata:
  name: employee-portal
  namespace: frontend
  labels:
    app: employee-portal
    atlasos.io/managed: "true"
spec:
  selector:
    app: employee-portal
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
