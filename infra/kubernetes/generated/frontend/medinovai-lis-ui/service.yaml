apiVersion: v1
kind: Service
metadata:
  name: medinovai-lis-ui
  namespace: frontend
  labels:
    app: medinovai-lis-ui
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-lis-ui
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
