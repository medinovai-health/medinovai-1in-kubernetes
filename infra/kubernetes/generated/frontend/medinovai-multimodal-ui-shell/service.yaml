apiVersion: v1
kind: Service
metadata:
  name: medinovai-multimodal-ui-shell
  namespace: frontend
  labels:
    app: medinovai-multimodal-ui-shell
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-multimodal-ui-shell
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
