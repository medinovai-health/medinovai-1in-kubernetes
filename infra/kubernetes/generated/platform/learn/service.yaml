apiVersion: v1
kind: Service
metadata:
  name: learn
  namespace: platform
  labels:
    app: learn
    atlasos.io/managed: "true"
spec:
  selector:
    app: learn
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
