apiVersion: v1
kind: Service
metadata:
  name: atlas-command
  namespace: platform
  labels:
    app: atlas-command
    atlasos.io/managed: "true"
spec:
  selector:
    app: atlas-command
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
