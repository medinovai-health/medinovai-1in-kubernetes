apiVersion: v1
kind: Service
metadata:
  name: gitlabfederation
  namespace: platform
  labels:
    app: gitlabfederation
    atlasos.io/managed: "true"
spec:
  selector:
    app: gitlabfederation
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
