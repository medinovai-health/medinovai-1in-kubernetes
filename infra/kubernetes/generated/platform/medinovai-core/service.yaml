apiVersion: v1
kind: Service
metadata:
  name: medinovai-core
  namespace: platform
  labels:
    app: medinovai-core
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-core
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
