apiVersion: v1
kind: Service
metadata:
  name: medinovai-registry
  namespace: platform
  labels:
    app: medinovai-registry
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-registry
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
