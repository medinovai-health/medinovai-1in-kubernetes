apiVersion: v1
kind: Service
metadata:
  name: medinovai-canary-rollout-orchestrator
  namespace: platform
  labels:
    app: medinovai-canary-rollout-orchestrator
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-canary-rollout-orchestrator
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
