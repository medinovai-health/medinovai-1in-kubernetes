apiVersion: v1
kind: Service
metadata:
  name: visionclaw
  namespace: platform
  labels:
    app: visionclaw
    atlasos.io/managed: "true"
spec:
  selector:
    app: visionclaw
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
