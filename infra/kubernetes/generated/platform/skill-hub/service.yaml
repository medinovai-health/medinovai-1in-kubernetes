apiVersion: v1
kind: Service
metadata:
  name: skill-hub
  namespace: platform
  labels:
    app: skill-hub
    atlasos.io/managed: "true"
spec:
  selector:
    app: skill-hub
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
