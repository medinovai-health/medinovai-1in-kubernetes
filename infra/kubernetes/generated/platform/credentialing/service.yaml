apiVersion: v1
kind: Service
metadata:
  name: credentialing
  namespace: platform
  labels:
    app: credentialing
    atlasos.io/managed: "true"
spec:
  selector:
    app: credentialing
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
