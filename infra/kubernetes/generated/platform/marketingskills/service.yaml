apiVersion: v1
kind: Service
metadata:
  name: marketingskills
  namespace: platform
  labels:
    app: marketingskills
    atlasos.io/managed: "true"
spec:
  selector:
    app: marketingskills
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
