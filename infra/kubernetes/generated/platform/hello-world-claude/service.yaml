apiVersion: v1
kind: Service
metadata:
  name: hello-world-claude
  namespace: platform
  labels:
    app: hello-world-claude
    atlasos.io/managed: "true"
spec:
  selector:
    app: hello-world-claude
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
