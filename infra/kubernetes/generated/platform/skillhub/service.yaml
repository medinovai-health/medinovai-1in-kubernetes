apiVersion: v1
kind: Service
metadata:
  name: skillhub
  namespace: platform
  labels:
    app: skillhub
    atlasos.io/managed: "true"
spec:
  selector:
    app: skillhub
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
