apiVersion: v1
kind: Service
metadata:
  name: salesrag
  namespace: platform
  labels:
    app: salesrag
    atlasos.io/managed: "true"
spec:
  selector:
    app: salesrag
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
