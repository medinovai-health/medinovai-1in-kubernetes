apiVersion: v1
kind: Service
metadata:
  name: medinovai-sales
  namespace: platform
  labels:
    app: medinovai-sales
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-sales
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
