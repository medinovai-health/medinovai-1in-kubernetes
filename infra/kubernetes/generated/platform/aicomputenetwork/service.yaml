apiVersion: v1
kind: Service
metadata:
  name: aicomputenetwork
  namespace: platform
  labels:
    app: aicomputenetwork
    atlasos.io/managed: "true"
spec:
  selector:
    app: aicomputenetwork
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
