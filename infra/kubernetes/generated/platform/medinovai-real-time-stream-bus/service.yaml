apiVersion: v1
kind: Service
metadata:
  name: medinovai-real-time-stream-bus
  namespace: platform
  labels:
    app: medinovai-real-time-stream-bus
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-real-time-stream-bus
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
