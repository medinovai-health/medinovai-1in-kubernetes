apiVersion: v1
kind: Service
metadata:
  name: crm
  namespace: platform
  labels:
    app: crm
    atlasos.io/managed: "true"
spec:
  selector:
    app: crm
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
