apiVersion: v1
kind: Service
metadata:
  name: docugeniepro
  namespace: platform
  labels:
    app: docugeniepro
    atlasos.io/managed: "true"
spec:
  selector:
    app: docugeniepro
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
