apiVersion: v1
kind: Service
metadata:
  name: autograntpro
  namespace: platform
  labels:
    app: autograntpro
    atlasos.io/managed: "true"
spec:
  selector:
    app: autograntpro
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
