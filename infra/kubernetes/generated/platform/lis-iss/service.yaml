apiVersion: v1
kind: Service
metadata:
  name: lis-iss
  namespace: platform
  labels:
    app: lis-iss
    atlasos.io/managed: "true"
spec:
  selector:
    app: lis-iss
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
