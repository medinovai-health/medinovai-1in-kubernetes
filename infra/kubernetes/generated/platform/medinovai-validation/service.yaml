apiVersion: v1
kind: Service
metadata:
  name: medinovai-validation
  namespace: platform
  labels:
    app: medinovai-validation
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-validation
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
