apiVersion: v1
kind: Service
metadata:
  name: medinovai-cortex
  namespace: platform
  labels:
    app: medinovai-cortex
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-cortex
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
