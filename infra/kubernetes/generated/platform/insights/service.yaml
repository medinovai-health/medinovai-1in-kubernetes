apiVersion: v1
kind: Service
metadata:
  name: insights
  namespace: platform
  labels:
    app: insights
    atlasos.io/managed: "true"
spec:
  selector:
    app: insights
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
