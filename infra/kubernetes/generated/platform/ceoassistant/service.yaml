apiVersion: v1
kind: Service
metadata:
  name: ceoassistant
  namespace: platform
  labels:
    app: ceoassistant
    atlasos.io/managed: "true"
spec:
  selector:
    app: ceoassistant
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
