apiVersion: v1
kind: Service
metadata:
  name: mywork
  namespace: platform
  labels:
    app: mywork
    atlasos.io/managed: "true"
spec:
  selector:
    app: mywork
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
