apiVersion: v1
kind: Service
metadata:
  name: ai-chatbot
  namespace: platform
  labels:
    app: ai-chatbot
    atlasos.io/managed: "true"
spec:
  selector:
    app: ai-chatbot
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
