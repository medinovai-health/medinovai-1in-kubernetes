apiVersion: v1
kind: Service
metadata:
  name: mywork-windsurf
  namespace: platform
  labels:
    app: mywork-windsurf
    atlasos.io/managed: "true"
spec:
  selector:
    app: mywork-windsurf
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
