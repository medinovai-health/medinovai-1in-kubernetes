apiVersion: v1
kind: Service
metadata:
  name: expansionplaybook
  namespace: platform
  labels:
    app: expansionplaybook
    atlasos.io/managed: "true"
spec:
  selector:
    app: expansionplaybook
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
