apiVersion: v1
kind: Service
metadata:
  name: mediovai
  namespace: platform
  labels:
    app: mediovai
    atlasos.io/managed: "true"
spec:
  selector:
    app: mediovai
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
