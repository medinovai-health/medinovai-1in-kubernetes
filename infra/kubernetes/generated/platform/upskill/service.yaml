apiVersion: v1
kind: Service
metadata:
  name: upskill
  namespace: platform
  labels:
    app: upskill
    atlasos.io/managed: "true"
spec:
  selector:
    app: upskill
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
