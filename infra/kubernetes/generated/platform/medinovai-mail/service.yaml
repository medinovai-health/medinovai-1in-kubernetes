apiVersion: v1
kind: Service
metadata:
  name: medinovai-mail
  namespace: platform
  labels:
    app: medinovai-mail
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-mail
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
