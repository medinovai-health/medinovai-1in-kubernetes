apiVersion: v1
kind: Service
metadata:
  name: myonsiteops
  namespace: platform
  labels:
    app: myonsiteops
    atlasos.io/managed: "true"
spec:
  selector:
    app: myonsiteops
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
