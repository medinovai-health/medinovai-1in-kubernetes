apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: platform
  labels:
    app: openclaw
    atlasos.io/managed: "true"
spec:
  selector:
    app: openclaw
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
