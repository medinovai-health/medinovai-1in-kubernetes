apiVersion: v1
kind: Service
metadata:
  name: medinovai-regulatory-submissions
  namespace: clinical
  labels:
    app: medinovai-regulatory-submissions
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-regulatory-submissions
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
