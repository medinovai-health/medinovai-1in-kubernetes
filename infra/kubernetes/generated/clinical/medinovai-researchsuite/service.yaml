apiVersion: v1
kind: Service
metadata:
  name: medinovai-researchsuite
  namespace: clinical
  labels:
    app: medinovai-researchsuite
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-researchsuite
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
