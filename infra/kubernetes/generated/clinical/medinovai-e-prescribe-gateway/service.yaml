apiVersion: v1
kind: Service
metadata:
  name: medinovai-e-prescribe-gateway
  namespace: clinical
  labels:
    app: medinovai-e-prescribe-gateway
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-e-prescribe-gateway
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
