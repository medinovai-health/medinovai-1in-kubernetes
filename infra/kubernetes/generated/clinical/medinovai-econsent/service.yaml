apiVersion: v1
kind: Service
metadata:
  name: medinovai-econsent
  namespace: clinical
  labels:
    app: medinovai-econsent
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-econsent
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
