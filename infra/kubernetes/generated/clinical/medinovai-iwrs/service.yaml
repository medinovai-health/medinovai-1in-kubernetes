apiVersion: v1
kind: Service
metadata:
  name: medinovai-iwrs
  namespace: clinical
  labels:
    app: medinovai-iwrs
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-iwrs
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
