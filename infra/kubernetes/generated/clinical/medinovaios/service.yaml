apiVersion: v1
kind: Service
metadata:
  name: medinovaios
  namespace: clinical
  labels:
    app: medinovaios
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovaios
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
