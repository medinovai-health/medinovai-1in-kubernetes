apiVersion: v1
kind: Service
metadata:
  name: medinovai-telehealth-hub
  namespace: clinical
  labels:
    app: medinovai-telehealth-hub
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-telehealth-hub
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
