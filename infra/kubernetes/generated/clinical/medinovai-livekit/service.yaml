apiVersion: v1
kind: Service
metadata:
  name: medinovai-livekit
  namespace: clinical
  labels:
    app: medinovai-livekit
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-livekit
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
