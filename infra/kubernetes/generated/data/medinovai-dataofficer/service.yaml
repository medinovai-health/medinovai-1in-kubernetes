apiVersion: v1
kind: Service
metadata:
  name: medinovai-dataofficer
  namespace: data
  labels:
    app: medinovai-dataofficer
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-dataofficer
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
