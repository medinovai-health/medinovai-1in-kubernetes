apiVersion: v1
kind: Service
metadata:
  name: medinovaigent
  namespace: intelligence
  labels:
    app: medinovaigent
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovaigent
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
