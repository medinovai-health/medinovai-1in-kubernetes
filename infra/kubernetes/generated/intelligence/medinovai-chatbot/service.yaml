apiVersion: v1
kind: Service
metadata:
  name: medinovai-chatbot
  namespace: intelligence
  labels:
    app: medinovai-chatbot
    atlasos.io/managed: "true"
spec:
  selector:
    app: medinovai-chatbot
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
