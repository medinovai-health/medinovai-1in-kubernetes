apiVersion: v1
kind: Service
metadata:
  name: mcp-deploy
  namespace: medinovai
  labels:
    app.kubernetes.io/name: mcp-deploy
    app.kubernetes.io/part-of: atlasos
spec:
  type: ClusterIP
  ports:
    - port: 3120
      targetPort: 3120
      protocol: TCP
      name: http
  selector:
    app: mcp-deploy
