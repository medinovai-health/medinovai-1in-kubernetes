apiVersion: v1
kind: Service
metadata:
  name: rbac-guard
  namespace: medinovai
  labels:
    app.kubernetes.io/name: rbac-guard
    app.kubernetes.io/part-of: atlasos
spec:
  type: ClusterIP
  ports:
    - port: 3113
      targetPort: 3113
      protocol: TCP
      name: http
  selector:
    app: rbac-guard
