apiVersion: v1
kind: Service
metadata:
  name: agent-provisioner
  namespace: medinovai
  labels:
    app.kubernetes.io/name: agent-provisioner
    app.kubernetes.io/part-of: atlasos
spec:
  type: ClusterIP
  ports:
    - port: 3111
      targetPort: 3111
      protocol: TCP
      name: http
  selector:
    app: agent-provisioner
