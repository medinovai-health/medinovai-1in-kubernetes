apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
nameSuffix: -stage
namespace: medinovai-stage
resources:
  - ../../base
patches:
  - target: { kind: Deployment, name: APP_NAME }
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
