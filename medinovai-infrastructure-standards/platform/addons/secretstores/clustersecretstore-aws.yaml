apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: org-aws-secrets-manager
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: eso-aws
            namespace: external-secrets
