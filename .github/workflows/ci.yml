name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  use-standard-ci:
    uses: ./.github/workflows/standard-ci.yml
    with:
      coverage-threshold: 80
